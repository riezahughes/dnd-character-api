# Migration `20201020112730-init`

This migration has been generated by Dan at 10/20/2020, 12:27:30 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Character" (
"id" SERIAL,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"name" text   ,
"raceId" integer   NOT NULL ,
"classId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Race" (
"id" SERIAL,
"name" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Class" (
"id" SERIAL,
"name" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."SubClass" (
"id" SERIAL,
"name" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Weapons" (
"id" SERIAL,
"name" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Items" (
"id" SERIAL,
"name" text   ,
PRIMARY KEY ("id")
)

ALTER TABLE "public"."Character" ADD FOREIGN KEY ("raceId")REFERENCES "public"."Race"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Character" ADD FOREIGN KEY ("classId")REFERENCES "public"."Class"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201020111617-init..20201020112730-init
--- datamodel.dml
+++ datamodel.dml
@@ -2,10 +2,45 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
+
+model Character{
+  id  Int @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  name String?
+  race  Race  @relation(fields: [raceId], references: [id])
+  raceId Int
+  class Class @relation(fields: [classId], references: [id])
+  classId Int
+}
+
+model Race{
+  id  Int @default(autoincrement()) @id
+  name  String?
+}
+
+model Class{
+  id  Int @default(autoincrement()) @id
+  name String?
+}
+
+model SubClass{
+  id  Int @default(autoincrement()) @id
+  name String?
+}
+
+model Weapons{
+  id  Int @default(autoincrement()) @id
+  name String?
+}
+
+model Items{
+  id  Int @default(autoincrement()) @id
+  name String?
+}
```


